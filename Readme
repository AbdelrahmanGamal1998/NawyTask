# Demoblaze Automation Project

This project is a simple test automation suite to automate several scenarios on the Demoblaze website using the SHAFT engine framework. The project uses separate JSON files for test data, Page Object Model (POM) design pattern, anonymous objects to call methods, and Allure for reporting.

## Project Structure